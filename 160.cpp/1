#include<iostream>
#include<iomanip>
#include<math.h>
#define pai 3.14
using namespace std;

int main() {
	float r, h;
	cin >> r >> h;
	float s1, s2;
	cout.setf(ios::fixed);
	cout << setprecision(2) << pow(r, 2) * (2 + pai / 2) * 2 + h * r * (pai + 2 + 2 * sqrt(2));
}


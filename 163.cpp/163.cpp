#include<iostream>
#include<iomanip>
#include<math.h>
using namespace std;
int main() {
	double a, b, c;
	cin >> a >> b >> c;
	cout.setf(ios::fixed);
	cout << setprecision(6) << sqrt(a * a + b * b -2 * a * b * cos(c * acos(-1)  / 180));
}
